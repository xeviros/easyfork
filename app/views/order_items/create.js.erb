// SELECT ITEM
// MODIFY
if ( $(window).width() > 601) {
  //Add your javascript for large screens here
  function addOrder(htmlPartial) {
    const userOrders = document.querySelector("#user-orders-desk")
    userOrders.insertAdjacentHTML("beforeend", htmlPartial)
  }
  addOrder("<%= j render 'order_item', order_item: @order_item %>");

  function updatePriceMob() {
    // SELECT PRICE
    // UPDATE THE PRICE
    const updatePriceMob = document.querySelector("[data-order-price-id='<%= @order.id %>']");
    updatePriceMob.innerHTML = "<%= humanized_money_with_symbol(@order.amount) %>"
  }

  updatePriceMob()




}
else {
  //Add your javascript for small screens here
  function addOrderMob(htmlPartial) {
    const userOrdersMob = document.querySelector("#user-orders-mob")
    userOrdersMob.insertAdjacentHTML("beforeend", htmlPartial)
  }
  addOrderMob("<%= j render 'order_item', order_item: @order_item %>");
  function updatePriceMob() {
  // SELECT PRICE
  // UPDATE THE PRICE
    const updatePriceMob = document.querySelector("[data-order-price-id-mob='<%= @order.id %>']");
    updatePriceMob.innerHTML = "<%= humanized_money_with_symbol(@order.amount) %>"
  }

  updatePriceMob()
}







