<%#items_array = @bill.restaurant.items %>

<% @bill.restaurant.items.each do |item| %>
  <%= link_to item.name, order_order_items_path(@order, item_id: item.id), method: :post %>
  <br>
<% end %>

<p> Hello - I'm the bill show page</p>


<%= simple_form_for [@order, @order_item] do |f| %>
  <%= f.association :item, :collection => @bill.restaurant.items %>
  <%= f.button :submit %>
<% end %>

<%# @bill.orders.each do |order| %>
  <%#= order.order_items.each do |item| %>
    <%#= item.name %>
  <%# end %>
<%# end %>

