function updateItems() {
  // console.log(<%= @item%>)
  const container = document.querySelector(".menu-items");
  container.innerHTML = "<%= j render 'items/restaurant-items', item: @item, restaurant: @restaurant %>"
  // container.innerHTML = "<%= @item%>"
}
updateItems();
