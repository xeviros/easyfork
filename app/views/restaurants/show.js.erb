function updateItems() {
  const container = document.querySelector(".menu-items");
  container.innerHTML = "<%= j render 'items/restaurant-items', item: @item, restaurant: @restaurant, order: @order, order_item: @order_item, bill: @bill %>"
}
updateItems();
